import styled from "styled-components"
import FullContainer from "./FullContainer"

const FullContainer_Styled = styled(FullContainer)`
  display: block;
  max-width: 1440px;
  margin-left: auto;
  margin-right: auto;
  position: relative;
  width: 100%;
`

export default FullContainer_Styled
